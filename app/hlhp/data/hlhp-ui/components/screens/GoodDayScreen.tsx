// Removed in v2 — the Good Day tab was replaced by the Learn screen
// (LearnScreen.tsx): symptom explainers + the SkinBB knowledge article feed,
// personalized to today's drivers and what the user has logged. This file is
// intentionally left as an empty module; nothing imports it. Safe to delete.
export {};
