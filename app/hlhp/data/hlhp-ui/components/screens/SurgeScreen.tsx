// Removed in v2 — the Surge tab was folded into the enhanced Hello screen
// (animated SFI, mode badge incl. Hostile Mode / Code Red, impact lines, and
// the L0→L1 flash alert with the surge demo toggle). This file is intentionally
// left as an empty module; nothing imports it. Safe to delete.
export {};
