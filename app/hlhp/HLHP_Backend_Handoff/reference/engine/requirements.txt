# HLHP backend — Python deps (from backend/README.md)
# Tests run with no DB/Redis (they use mock_cache + the bundled xlsx).
pytest
pytest-asyncio
openpyxl
pydantic
fastapi
uvicorn
# Production extras (engagement service + engine against a live store):
motor          # MongoDB async driver
redis
